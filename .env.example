# =============================================================================
# Dashboard Nutrition & Sport — Variables d'environnement
# =============================================================================
# Copier ce fichier en .env.local et remplir les valeurs réelles.
# Ne jamais committer .env.local dans Git.
# =============================================================================


# -----------------------------------------------------------------------------
# SUPABASE — V1 (obligatoire dès le départ)
# Récupérer ces valeurs dans : Supabase > Project Settings > API
# -----------------------------------------------------------------------------

# URL publique du projet Supabase (non-sensible, utilisable côté client)
NEXT_PUBLIC_SUPABASE_URL=https://<project-id>.supabase.co

# Clé anonyme publique (lecture publique, non-sensible)
NEXT_PUBLIC_SUPABASE_ANON_KEY=<supabase-anon-key>

# Clé service role — SERVEUR UNIQUEMENT, ne jamais préfixer NEXT_PUBLIC_
# Utilisée exclusivement dans les Server Actions Next.js
SUPABASE_SERVICE_ROLE_KEY=<supabase-service-role-key>


# -----------------------------------------------------------------------------
# SUUNTO API — V2 (connexion montre sportive, optionnel en V1)
# Récupérer ces valeurs sur : https://www.suunto.com/api/
# -----------------------------------------------------------------------------

SUUNTO_CLIENT_ID=<suunto-oauth-client-id>
SUUNTO_CLIENT_SECRET=<suunto-oauth-client-secret>

# URL de callback OAuth — doit correspondre à ce qui est enregistré chez Suunto
# Exemple local : http://localhost:3000/api/suunto/callback
SUUNTO_REDIRECT_URI=<https://votre-domaine.vercel.app/api/suunto/callback>


# -----------------------------------------------------------------------------
# INTERMARCHÉ DRIVE API — V3 (remplissage panier automatique, optionnel en V1/V2)
# -----------------------------------------------------------------------------

DRIVE_API_BASE_URL=<https://api.intermarche.fr/drive/v1>
DRIVE_API_KEY=<drive-api-key>
DRIVE_STORE_ID=<identifiant-magasin-drive>


# -----------------------------------------------------------------------------
# APPLICATION
# -----------------------------------------------------------------------------

# Environnement : development | production
NODE_ENV=development

# URL publique de l'application (utilisée pour les redirections OAuth)
NEXT_PUBLIC_APP_URL=http://localhost:3000
